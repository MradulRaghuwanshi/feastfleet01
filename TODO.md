# TODO

- [ ] Generate fresh React Native shell (FeastFleetApp-rn) under `food-delivery/`
- [ ] Copy JS/app folders (src, components, screens, firebase, utils, assets if present) from existing `food-delivery/rn-app/` into new shell
- [ ] Copy/merge dependencies: ensure new shell `package.json` matches existing `food-delivery/rn-app/package.json`
- [x] Copy `google-services.json` into `android/app/google-services.json`
- [ ] Run `npm install` in new shell
- [ ] Validate Android debug build, then build release APK


